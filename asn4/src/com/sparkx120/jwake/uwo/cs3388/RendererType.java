package com.sparkx120.jwake.uwo.cs3388;

public enum RendererType {
	/**
	 * Object Data is for renderers that use Object data translated into camera coordinates
	 * Camera Pipe is for renderers that need access to the camera pipe to compute pixel data
	 */
	OBJECT_DATA, 
	CAMERA_PIPE
}
